{
  "name": "scraping-bot",
  "version": "1.0.0",
  "description": "TypeScript scraper for JSF-based jurisprudencia portal",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "scrape": "ts-node src/index.ts",
    "scrape:fresh": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"if (Test-Path -LiteralPath 'runs') { Remove-Item -LiteralPath 'runs' -Recurse -Force }; if (Test-Path -LiteralPath 'output') { Remove-Item -LiteralPath 'output' -Recurse -Force }; New-Item -ItemType Directory -Path 'runs' | Out-Null; New-Item -ItemType Directory -Path 'output' | Out-Null; npm run scrape -- --bot civil --search civil\"",
    "reliability:campaign": "node scripts/reliability-campaign.js",
    "reliability:report": "node scripts/reliability-campaign.js --report-only",
    "reliability:self-check": "node scripts/reliability-campaign.js --self-check",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@types/adm-zip": "^0.5.8",
    "adm-zip": "^0.6.0",
    "axios": "^1.19.0",
    "axios-cookiejar-support": "^7.0.0",
    "cheerio": "^1.2.0",
    "fast-xml-parser": "^5.11.0",
    "tough-cookie": "^6.0.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitest/coverage-v8": "^4.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11"
  }
}
