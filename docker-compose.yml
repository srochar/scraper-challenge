services:
  bot-runner:
    build: .
    container_name: scraper-bot-runner
    command: ["npm", "run", "scrape", "--", "--bot", "civil", "--search", "civil"]
    volumes:
      - ./runs:/app/runs
    restart: "no"
